# Changelog

## 0.6.3

- 修复 CI Clippy：`sort_by` 改为 `sort_by_key`

## 0.6.2

### 可靠性

- 写入 `*.custom.yaml` 时保留未改动键的原文和注释，不再整文件重序列化
- 读取配置只走 YAML 解析，去掉按行正则回退
- 配置中心支持 `.txt`、`.lua` 和用户目录子路径，并拦截路径穿越
- 短语保存保留当前顺序，不再按权重重写文件
- 手动备份可写备注；恢复时写回备份内的全部配置文件；支持与当前目录 diff

### 体验

- 短语和未启用词库表分页
- 部署过程推送日志，状态栏可取消部署
- 概览页展示 `*.userdb` 和 `sync/` 目录
- 快速设置不再默认绑定雾凇；未安装雾凇时隐藏组件开关
- Hash 路由，刷新后停留在当前页
- 系统深色偏好变化时，未手动选主题会跟随

### 工程

- 拆分词库后端模块与主题预设数据
- 删除未使用的 `useBackup` / `useDeploy`
- HTTP 请求仅对 GitHub 带 `application/vnd.github+json`
- IPC 错误带 `code` + `message`；前端 Vitest 覆盖短语去重与错误格式化
